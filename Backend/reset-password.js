echo const bcrypt = require('bcryptjs'); const { db } = require('./src/db/connection'); const newHash = bcrypt.hashSync('S1mpl3L1n3', 10); db.prepare('UPDATE users SET password_hash = ? WHERE role = "SuperAdmin"').run(newHash); console.log('Listo'); > reset-password.js
